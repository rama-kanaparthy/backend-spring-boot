server:
  port: 8080

spring:
  profiles:
    active: dev

#  datasource:
#    url: jdbc:mysql://localhost:3306/mydatabase
#    username: myuser
#    password: mypassword
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password
    platform: h2

    jpa:
      hibernate:
        ddl-auto: update

    h2:
      console:
        enabled: true
        path: /h2-console